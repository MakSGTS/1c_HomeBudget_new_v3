
Процедура ЗагрузитьДанные(ЗагрузкаССервера, ИмяПользователя = Неопределено, Пароль = Неопределено, ТолькоСправочники = Ложь) Экспорт
	
	Если ЗагрузкаССервера Тогда
		СборСтатистикиСервер.ЗаписатьФактИспользованияСервиса(ПредопределенноеЗначение("Перечисление.ВидыСтатистики.ВосстановлениеДанныхОблако"), Истина);
		Путь = ПолучитьИмяВременногоФайла("txt");
		ЗапросыHTTP_Сервер.ПолучитьДанныеССервера(Путь, ИмяПользователя, Пароль);
	Иначе
		//TODO: Здесь неоюходимо делать полный бэкап?!!!
		СборСтатистикиСервер.ЗаписатьФактИспользованияСервиса(ПредопределенноеЗначение("Перечисление.ВидыСтатистики.ВосстановлениеДанныхЛокально"), Истина);
		Путь = КаталогДокументов() + "\backUp_1CDB.txt";
	КонецЕсли;
	ЗагрузкаДанныхСервер.ЗагрузитьДанные(Путь, ТолькоСправочники);	
КонецПроцедуры


